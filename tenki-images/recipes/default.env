# Host-side build metadata for the default worker image, the one every repository without
# a recipe of its own runs. No repo backs it, so REPO_SLUG stays empty and there is no canary.
IMAGE_NAME=default
REPO_SLUG=
SANDBOX_CPU=4
SANDBOX_MEMORY_MB=8192
SANDBOX_DISK_GB=40
